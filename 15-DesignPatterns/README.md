# 15 - Design Patterns in AL

| Pattern | Use Case |
|---------|----------|
| Factory | Create objects without specifying concrete class |
| Strategy | Swap algorithms at runtime via Interfaces |
| Singleton | One shared instance via SingleInstance codeunit |
| Repository | Encapsulate all data access in one place |